package server

import (
	"github.com/gin-gonic/gin"
)

// RegisterRoutes registers all Stork API routes. All API routes are served from /stork/api.
func RegisterRoutes(router *gin.Engine, config StorkConfig) {
	// API Routes
}
